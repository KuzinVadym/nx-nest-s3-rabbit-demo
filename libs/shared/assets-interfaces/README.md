# assets-interfaces

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build assets-interfaces` to build the library.

## Running unit tests

Run `nx test assets-interfaces` to execute the unit tests via [Jest](https://jestjs.io).
